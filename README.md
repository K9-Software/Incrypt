# Incrypt A0.1-A0

>     import Incrypt
> 
>     data = "Hello World"
>     encrypted = Incrypt.encrypt(data)
>     decrypted = Incrypt.decrypt(encrypted)
> 
>     print(encrypted)
>     print(decrypted)
